import * as tfvis from '@tensorflow/tfjs-vis';

export async function trainModel() {
  // Get a surface
  /*const surface = */tfvis.visor().surface({tab: 'Model training', name: 'Actions'});
  // tfvis.show.modelSummary(surface, model);
}
