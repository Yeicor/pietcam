name: "CI"

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    name: "CI using Node.js ${{ matrix.node-version }}"
    runs-on: "ubuntu-latest"

    strategy:
      matrix:
        node-version: [ "20.x" ]

    steps:

    - uses: "actions/checkout@v3"

    - uses: "actions/setup-node@v3"
      with:
        node-version: "${{ matrix.node-version }}"
        cache: "yarn"
        cache-dependency-path: "./yarn.lock"

    - run: "yarn install --frozen-lockfile"
    - run: "yarn test"